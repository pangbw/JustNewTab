## 1. 项目结构搭建

- [x] 1.1 创建书签区域组件目录结构 (`src/components/BookmarkArea/`)
- [x] 1.2 创建书签数据 store 目录 (`src/stores/`)
- [x] 1.3 创建书签类型定义文件 (`src/types/bookmark.ts`)
- [x] 1.4 创建书签工作区类型定义 (`src/types/workspace.ts`)
- [x] 1.5 创建书签区域样式文件 (`src/styles/bookmark-area.css`)
- [x] 1.6 更新 manifest.json 添加书签权限 (`bookmarks`)
- [x] 1.7 更新 manifest.json 添加标签页权限 (`tabs`)

## 2. 数据层实现

- [x] 2.1 实现书签数据 store (`src/stores/bookmarkStore.ts`)
  - 创建书签块数据结构
  - 实现书签块的 CRUD 操作
  - 实现不可变数据更新
- [x] 2.2 实现工作区 store (`src/stores/workspaceStore.ts`)
  - 创建工作区数据结构
  - 实现工作区切换和创建
  - 实现工作区持久化到 localStorage
- [x] 2.3 实现拖拽状态 store (`src/stores/dragStore.ts`)
  - 创建拖拽状态数据结构
  - 实现拖拽开始/进行中/结束状态管理
- [x] 2.4 实现书签 API 适配器 (`src/adapters/bookmarkAdapter.ts`)
  - 使用 webextension-polyfill 封装书签 API
  - 处理 Chrome 和 Firefox API 差异
  - 实现书签树获取、搜索、创建、删除
- [x] 2.5 实现数据持久化工具 (`src/utils/storage.ts`)
  - 封装 localStorage 操作
  - 实现数据序列化/反序列化
  - 实现数据压缩（如果需要）
- [x] 2.6 实现标签页状态 store (`src/stores/tabStore.ts`)
  - 创建标签页状态数据结构
  - 实现标签页查询和监听
  - 实现已打开 URL 的 Set 数据结构
  - 实现防抖处理标签页变化事件

## 3. 工作区管理实现

- [x] 3.1 创建工作区页签组件 (`src/components/BookmarkArea/WorkspaceTabs.svelte`)
  - 显示工作区页签列表
  - 实现页签切换功能
  - 实现页签高亮显示
- [x] 3.2 实现工作区创建功能
  - 添加 "创建工作区" 按钮
  - 实现工作区命名对话框
  - 实现工作区创建逻辑
- [x] 3.3 实现工作区删除功能
  - 添加右键菜单删除选项
  - 实现删除确认对话框
  - 防止删除默认工作区
- [x] 3.4 实现工作区重命名功能
  - 双击页签进入编辑模式
  - 实现 Enter/Escape 快捷键
  - 实现点击外部保存
- [x] 3.5 添加工作区数量限制
  - 限制最大工作区数量 (20)
  - 显示达到限制的错误提示

## 4. 书签块管理实现

- [x] 4.1 创建书签块组件 (`src/components/BookmarkArea/BookmarkBlock.svelte`)
  - 显示书签块标题
  - 显示书签列表
  - 实现收起/展开功能
- [x] 4.2 创建单个书签项组件 (`src/components/BookmarkArea/BookmarkItem.svelte`)
  - 显示书签标题和图标
  - 实现点击打开书签
  - 实现拖拽支持
  - 显示已打开书签的高亮样式
  - 显示关闭按钮（已打开书签）
- [x] 4.3 实现书签块创建功能
  - 添加 "创建书签块" 按钮
  - 实现块命名对话框
  - 实现块颜色选择
- [x] 4.4 实现书签块编辑功能
  - 双击标题进入编辑模式
  - 实现保存和取消逻辑
- [x] 4.5 实现书签块删除功能
  - 添加右键菜单删除选项
  - 实现删除确认对话框
  - 实现级联删除书签
- [x] 4.6 实现书签块收起/展开功能
  - 添加收起/展开按钮
  - 实现平滑动画效果
  - 保存收起/展开状态
- [x] 4.7 添加书签块数量限制
  - 限制每个工作区最大块数 (20)
  - 显示达到限制的错误提示

## 5. 拖拽功能实现

- [x] 5.1 实现书签块拖拽
  - 添加 `draggable` 属性
  - 实现 `dragstart` 事件处理
  - 实现 `dragover` 事件处理
  - 实现 `drop` 事件处理
- [x] 5.2 实现拖拽视觉反馈
  - 显示拖拽幽灵元素
  - 显示放置区域高亮
  - 实现平滑动画效果
- [x] 5.3 实现拖拽位置保存
  - 保存拖拽后的位置到 store
  - 实现位置持久化
- [x] 5.4 处理无效拖拽
  - 取消拖出区域的拖拽
  - 返回原位置

## 6. 右键菜单实现

- [x] 6.1 创建右键菜单组件 (`src/components/BookmarkArea/ContextMenu.svelte`)
  - 显示菜单项列表
  - 实现菜单项点击事件
  - 实现菜单定位
- [x] 6.2 实现书签区域右键菜单
  - 拦截浏览器默认右键菜单
  - 显示 "创建块" 和 "创建书签" 选项
  - 实现菜单项执行逻辑
- [x] 6.3 实现书签块右键菜单
  - 显示 "编辑块"、"删除块"、"添加书签" 选项
  - 实现菜单项执行逻辑
- [x] 6.4 实现书签项右键菜单
  - 显示 "编辑书签"、"删除书签"、"在新标签页打开"、"复制 URL" 选项
  - 实现菜单项执行逻辑
- [x] 6.5 实现菜单关闭逻辑
  - 点击外部关闭菜单
  - 按 Escape 关闭菜单
  - 实现关闭动画

## 7. 书签快速编辑实现

- [x] 7.1 创建快速编辑卡片组件 (`src/components/BookmarkArea/QuickEdit.svelte`)
  - 显示编辑表单（显示内容、URL、描述）
  - 实现输入验证（zod）
  - 实现保存和取消逻辑
- [x] 7.2 实现显示标题编辑
  - 显示标题输入框
  - 实现输入验证（非空、最大100字符）
  - 实现保存逻辑
- [x] 7.3 实现 URL 编辑
  - 显示 URL 输入框
  - 实现 URL 格式验证
  - 实现协议自动补全（https://）
  - 实现保存逻辑
- [x] 7.4 实现描述编辑
  - 显示描述文本框
  - 实现字符计数显示（最大500字符）
  - 实现保存逻辑
- [x] 7.5 实现快速编辑卡片定位
  - 计算卡片显示位置（书签右侧）
  - 确保卡片在视口内
- [x] 7.6 实现快速编辑卡片关闭
  - 点击外部保存并关闭卡片
  - 按 Escape 取消并关闭卡片
  - 按 Enter 保存并关闭卡片
  - 实现关闭动画
- [x] 7.7 集成到书签项右键菜单
  - 修改书签项右键菜单，添加"快速编辑"选项
  - 点击"快速编辑"显示快速编辑卡片

## 8. 快速配置实现

- [x] 8.1 创建快速配置卡片组件 (`src/components/BookmarkArea/QuickConfig.svelte`)
  - 显示配置表单
  - 实现配置项编辑
  - 实现保存和取消逻辑
- [x] 8.2 实现块名称编辑
  - 显示名称输入框
  - 实现输入验证（非空、长度限制、唯一性）
  - 实现保存逻辑
- [x] 8.3 实现块颜色编辑
  - 显示颜色选择面板
  - 实现预设颜色选择
  - 实现自定义颜色选择
  - 实现颜色保存
- [x] 8.4 实现块图标编辑
  - 显示图标选择面板
  - 实现图标选择
  - 实现图标保存
- [x] 8.5 实现块布局选项
  - 显示布局选项（网格、列表）
  - 实现布局切换
  - 实现布局保存
- [x] 8.6 实现配置卡片定位
  - 计算卡片显示位置
  - 确保卡片在视口内
- [x] 8.7 实现配置卡片关闭
  - 点击外部关闭卡片
  - 按 Escape 关闭卡片
  - 实现关闭动画

## 9. 书签打开状态实现

- [x] 9.1 创建标签页状态 store (`src/stores/tabStore.ts`)
  - 创建标签页状态数据结构
  - 实现标签页查询和监听
  - 实现已打开 URL 的 Set 数据结构
  - 实现防抖处理标签页变化事件
- [x] 9.2 实现已打开书签检测
  - 使用 browser.tabs.query() 获取所有打开的标签页
  - 监听标签页创建、关闭、更新事件
  - 比较标签页 URL 与书签 URL
  - 更新书签的 isOpen 状态
- [x] 9.3 实现已打开书签高亮显示
  - 为已打开书签添加高亮样式（背景色、边框）
  - 实现高亮状态的平滑过渡动画
- [x] 9.4 实现已打开书签关闭按钮
  - 在已打开书签右侧显示关闭图标
  - 实现关闭按钮的点击事件
  - 调用 browser.tabs.remove() 关闭标签页
  - 实现关闭按钮的悬停效果
- [x] 9.5 实现 URL 匹配优化
  - 忽略协议差异（http/https）
  - 忽略尾部斜杠
  - 忽略 URL 片段（#）
  - 使用 Set 数据结构优化查找（O(1) 复杂度）

## 10. 书签导航行为实现

- [x] 10.1 实现默认导航行为
  - 左键点击在当前页面打开（window.location.href）
  - 显示加载状态指示器
  - 处理无效 URL 的错误提示
- [x] 10.2 实现键盘修饰符支持
  - Ctrl+点击（Windows/Linux）在新标签页打开
  - Cmd+点击（Mac）在新标签页打开
  - Shift+点击在新窗口打开
  - 中键点击在新标签页打开
- [x] 10.3 实现可配置导航模式
  - 在设置面板添加"书签打开方式"选项
  - 提供三个选项：当前页面、新标签页、新窗口
  - 默认设置为"当前页面"
- [x] 10.4 实现导航偏好持久化
  - 保存用户选择到 localStorage
  - 恢复保存的导航偏好
  - 应用设置到所有工作区
- [x] 10.5 实现书签悬停预览
  - 悬停时显示完整 URL 作为 tooltip
  - 显示格式：去除协议前缀（example.com/path）
- [x] 10.6 实现导航历史感知
  - 支持浏览器后退按钮返回新标签页
  - 保存工作区和滚动位置状态

## 11. 搜索功能实现

- [x] 11.1 创建搜索框组件 (`src/components/BookmarkArea/SearchBox.svelte`)
  - 显示搜索输入框
  - 显示搜索按钮
  - 显示清除按钮
- [x] 11.2 实现搜索执行逻辑
  - 按 Enter 执行搜索
  - 点击搜索按钮执行搜索
  - 实现防抖处理
- [x] 11.3 实现搜索结果显示
  - 显示匹配的书签
  - 按块分组显示
  - 显示无结果提示
- [x] 11.4 实现搜索关键词高亮
  - 高亮显示匹配文本
  - 支持标题和 URL 高亮
- [x] 11.5 实现实时搜索建议
  - 输入 3 个字符后显示建议
  - 显示匹配的书签建议
  - 点击建议执行搜索
- [x] 11.6 实现搜索历史
  - 记录最近 5 个搜索词
  - 显示搜索历史下拉列表
  - 实现清除历史功能
- [x] 11.7 实现搜索状态持久化
  - 保存最后搜索词
  - 恢复搜索结果
  - 切换工作区时清除搜索

## 12. 主容器组件实现

- [x] 12.1 创建书签区域主组件 (`src/components/BookmarkArea/BookmarkArea.svelte`)
  - 整合所有子组件
  - 实现布局管理
  - 实现响应式设计
- [x] 12.2 实现默认提示显示
  - 无书签块时显示提示
  - 提示文字："右键呼出菜单创建书签"
  - 创建块后隐藏提示
- [x] 12.3 实现响应式布局
  - 适配不同屏幕尺寸
  - 实现断点处理 (sm, md, lg, xl)
- [x] 12.4 集成到主页面
  - 更新 `src/newtab/App.svelte`
  - 添加书签区域组件
  - 调整页面布局

## 13. 浏览器兼容性处理

- [x] 13.1 测试 Chrome 浏览器兼容性
  - 测试所有功能在 Chrome 中正常工作
  - 修复 Chrome 特定问题
- [x] 13.2 测试 Edge 浏览器兼容性
  - 测试所有功能在 Edge 中正常工作
  - 修复 Edge 特定问题
- [x] 13.3 测试 Firefox 浏览器兼容性
  - 测试所有功能在 Firefox 中正常工作
  - 修复 Firefox 特定问题
  - 处理 Manifest V2/V3 差异
- [x] 13.4 处理浏览器 API 差异
  - 使用 webextension-polyfill 统一 API
  - 实现浏览器特定适配器
  - 测试 API 兼容性

## 14. 测试和验证

- [x] 14.1 编写单元测试
  - 测试 store 逻辑
  - 测试工具函数
  - 测试数据转换
- [x] 14.2 编写组件测试
  - 测试组件渲染
  - 测试用户交互
  - 测试事件处理
- [x] 14.3 进行集成测试
  - 测试组件间交互
  - 测试数据流
  - 测试状态管理
- [x] 14.4 进行性能测试
  - 测试大量书签的性能
  - 测试拖拽性能
  - 测试搜索性能
- [x] 14.5 进行用户体验测试
  - 测试响应式布局
  - 测试动画效果
  - 测试错误处理

## 15. 文档和优化

- [x] 15.1 编写组件文档
  - 说明组件用途
  - 说明组件 API
  - 说明使用示例
- [x] 15.2 优化代码质量
  - 移除 console.log 语句
  - 优化代码结构
  - 确保符合编码规范
- [x] 15.3 优化性能
  - 实现虚拟滚动（如果需要）
  - 优化渲染逻辑
  - 减少不必要的重绘
- [x] 15.4 优化扩展体积
  - 移除未使用代码
  - 压缩图片资源
  - 监控构建产物大小
- [x] 15.5 准备发布
  - 更新版本号
  - 更新 CHANGELOG
  - 准备发布说明
